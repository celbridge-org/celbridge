// Test stub for the document-api module. Mirrors only the symbols
// editor-controller.js consumes.

export const ContentLoadedReason = Object.freeze({
    Initial: 'Initial',
    ExternalReload: 'ExternalReload'
});
