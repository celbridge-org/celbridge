<UserControl
  x:Class="Celbridge.Search.Views.SearchPanel"
  xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
  xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
  xmlns:local="using:Celbridge.Search.Views"
  xmlns:vm="using:Celbridge.Search.ViewModels"
  xmlns:controls="using:Celbridge.UserInterface.Views.Controls"
  xmlns:ws="using:Celbridge.WorkspaceUI.Views"
  xmlns:converters="using:Celbridge.UserInterface.Converters"
  xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
  GotFocus="UserControl_GotFocus"
  PointerPressed="UserControl_PointerPressed">

  <UserControl.Resources>
    <converters:ExpanderGlyphConverter x:Key="ExpandCollapseGlyphConverter"/>
    <converters:BoolToBrushConverter x:Key="SelectionBackgroundConverter"
                                     TrueBrush="{ThemeResource ListItemSelectedBackgroundBrush}"
                                     FalseBrush="Transparent"/>
    <converters:CountToBoolConverter x:Key="CountToBoolConverter"/>
  </UserControl.Resources>

  <Grid VerticalAlignment="Stretch"
        Background="Transparent">

    <Grid.RowDefinitions>
      <RowDefinition Height="Auto"/>
      <RowDefinition Height="Auto"/>
      <RowDefinition Height="Auto"/>
      <RowDefinition Height="Auto"/>
      <RowDefinition Height="*"/>
    </Grid.RowDefinitions>

    <!-- Panel Header -->
    <ws:PanelHeader Grid.Row="0"
                    Panel="Search"
                    Region="Primary"
                    Title="{x:Bind ViewModel.TitleText, Mode=OneWay}"/>

    <!-- Search Input -->
    <Grid Grid.Row="1"
          Padding="12,8,12,4">
      <Grid.RowDefinitions>
        <RowDefinition Height="Auto"/>
        <RowDefinition Height="Auto"/>
      </Grid.RowDefinitions>
      <Grid.ColumnDefinitions>
        <ColumnDefinition Width="*"/>
        <ColumnDefinition Width="Auto"/>
        <ColumnDefinition Width="Auto"/>
      </Grid.ColumnDefinitions>

      <!-- Search label -->
      <TextBlock Grid.Row="0"
                 Grid.Column="0"
                 Text="{x:Bind ViewModel.SearchPlaceholder, Mode=OneWay}"
                 Margin="0,0,0,4"/>

      <TextBox x:Name="SearchTextBox"
               AutomationProperties.AutomationId="search-input"
               Grid.Row="1"
               Grid.Column="0"
               Text="{x:Bind ViewModel.SearchText, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
               KeyDown="SearchTextBox_KeyDown"
               LostFocus="SearchTextBox_LostFocus"/>

      <!-- Progress ring overlay -->
      <ProgressRing Grid.Row="1"
                    IsActive="{x:Bind ViewModel.IsSearching, Mode=OneWay}"
                    Width="16"
                    Height="16"
                    HorizontalAlignment="Right"
                    VerticalAlignment="Center"
                    Margin="0,0,8,0"/>

      <!-- Search History dropdown button -->
      <Button Grid.Row="1"
              Grid.Column="1"
              AutomationProperties.AutomationId="search-history-button"
              Padding="4,0"
              Margin="0"
              VerticalAlignment="Stretch"
              ToolTipService.ToolTip="{x:Bind ViewModel.SearchHistoryTooltip, Mode=OneWay}">
        <controls:Icon Symbol="ChevronDown" FontSize="12"/>
        <Button.Flyout>
          <MenuFlyout x:Name="SearchHistoryFlyout" Placement="BottomEdgeAlignedRight">
            <MenuFlyoutSeparator/>
            <MenuFlyoutItem Text="{x:Bind ViewModel.ClearHistoryText, Mode=OneWay}"
                            Command="{x:Bind ViewModel.ClearSearchHistoryCommand}"
                            IsEnabled="{x:Bind ViewModel.SearchHistory.Count, Mode=OneWay, Converter={StaticResource CountToBoolConverter}}">
              <MenuFlyoutItem.Icon>
                <controls:Icon Symbol="Delete"/>
              </MenuFlyoutItem.Icon>
            </MenuFlyoutItem>
          </MenuFlyout>
        </Button.Flyout>
      </Button>

      <!-- Search button -->
      <Button Grid.Row="1"
              Grid.Column="2"
              AutomationProperties.AutomationId="search-run-button"
              Command="{x:Bind ViewModel.ExecuteSearchCommand}"
              Padding="8,0"
              Margin="4,0,0,0"
              VerticalAlignment="Stretch"
              ToolTipService.ToolTip="{x:Bind ViewModel.SearchTooltip, Mode=OneWay}">
        <controls:Icon Symbol="Search" FontSize="14"/>
      </Button>
    </Grid>

    <!-- Search Options -->
    <StackPanel Grid.Row="2"
                Orientation="Horizontal"
                Padding="12,4,12,8"
                Spacing="4"
                HorizontalAlignment="Center">
      <!-- Match Case toggle button -->
      <ToggleButton IsChecked="{x:Bind ViewModel.MatchCase, Mode=TwoWay}"
                    AutomationProperties.AutomationId="search-match-case-button"
                    ToolTipService.ToolTip="{x:Bind ViewModel.MatchCaseTooltip, Mode=OneWay}"
                    Padding="8,6"
                    MinWidth="36"
                    MinHeight="32">
        <controls:Icon Symbol="MatchCase" FontSize="14"/>
      </ToggleButton>

      <!-- Whole Word toggle button -->
      <ToggleButton IsChecked="{x:Bind ViewModel.WholeWord, Mode=TwoWay}"
                    AutomationProperties.AutomationId="search-whole-word-button"
                    ToolTipService.ToolTip="{x:Bind ViewModel.WholeWordTooltip, Mode=OneWay}"
                    Padding="8,6"
                    MinWidth="36"
                    MinHeight="32">
        <TextBlock Text="ab" FontWeight="SemiBold"/>
      </ToggleButton>

      <!-- Collapse All button -->
      <Button Command="{x:Bind ViewModel.CollapseAllCommand}"
              AutomationProperties.AutomationId="search-collapse-results-button"
              ToolTipService.ToolTip="{x:Bind ViewModel.CollapseAllTooltip, Mode=OneWay}"
              IsEnabled="{x:Bind ViewModel.HasResults, Mode=OneWay}"
              Background="Transparent"
              BorderThickness="1"
              Padding="8,6"
              MinWidth="36"
              MinHeight="32">
        <controls:Icon Symbol="Collapse" FontSize="14"/>
      </Button>

      <!-- Replace Mode toggle button -->
      <ToggleButton IsChecked="{x:Bind ViewModel.IsReplaceModeEnabled, Mode=TwoWay}"
                    AutomationProperties.AutomationId="search-replace-toggle-button"
                    ToolTipService.ToolTip="{x:Bind ViewModel.ReplaceToggleTooltip, Mode=OneWay}"
                    Padding="8,6"
                    MinWidth="36"
                    MinHeight="32">
        <controls:Icon Symbol="Replace" FontSize="14"/>
      </ToggleButton>
    </StackPanel>

    <!-- Replace Input (visible only when replace mode is enabled) -->
    <Grid Grid.Row="3"
          Padding="12,0,12,8"
          Visibility="{x:Bind ViewModel.IsReplaceModeEnabled, Mode=OneWay}">
      <Grid.ColumnDefinitions>
        <ColumnDefinition Width="*"/>
        <ColumnDefinition Width="Auto"/>
        <ColumnDefinition Width="Auto"/>
      </Grid.ColumnDefinitions>

      <TextBox x:Name="ReplaceTextBox"
               Grid.Column="0"
               AutomationProperties.AutomationId="search-replace-input"
               Header="{x:Bind ViewModel.ReplacePlaceholder, Mode=OneWay}"
               Text="{x:Bind ViewModel.ReplaceText, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
               LostFocus="ReplaceTextBox_LostFocus"/>

      <!-- Replace History dropdown button -->
      <Button Grid.Column="1"
              AutomationProperties.AutomationId="search-replace-history-button"
              Padding="4,0"
              Margin="0"
              VerticalAlignment="Bottom"
              Height="32"
              ToolTipService.ToolTip="{x:Bind ViewModel.ReplaceHistoryTooltip, Mode=OneWay}">
        <controls:Icon Symbol="ChevronDown" FontSize="12"/>
        <Button.Flyout>
          <MenuFlyout x:Name="ReplaceHistoryFlyout" Placement="BottomEdgeAlignedRight">
            <MenuFlyoutSeparator/>
            <MenuFlyoutItem Text="{x:Bind ViewModel.ClearHistoryText, Mode=OneWay}"
                            Command="{x:Bind ViewModel.ClearSearchHistoryCommand}"
                            IsEnabled="{x:Bind ViewModel.ReplaceHistory.Count, Mode=OneWay, Converter={StaticResource CountToBoolConverter}}">
              <MenuFlyoutItem.Icon>
                <controls:Icon Symbol="Delete"/>
              </MenuFlyoutItem.Icon>
            </MenuFlyoutItem>
          </MenuFlyout>
        </Button.Flyout>
      </Button>

      <!-- Replace All button -->
      <Button Grid.Column="2"
              AutomationProperties.AutomationId="search-replace-all-button"
              Command="{x:Bind ViewModel.ReplaceAllCommand}"
              Padding="8,0"
              Margin="4,0,0,0"
              VerticalAlignment="Bottom"
              Height="32"
              IsEnabled="{x:Bind ViewModel.HasResults, Mode=OneWay}"
              ToolTipService.ToolTip="{x:Bind ViewModel.ReplaceAllTooltip, Mode=OneWay}">
        <controls:Icon Symbol="Replace" FontSize="14"/>
      </Button>
    </Grid>

    <!-- Results Section -->
    <Grid Grid.Row="4">
      <Grid.RowDefinitions>
        <RowDefinition Height="Auto"/>
        <RowDefinition Height="*"/>
      </Grid.RowDefinitions>

      <!-- Status Bar -->
      <Border Grid.Row="0"
              Padding="12,6"
              Visibility="{x:Bind ViewModel.HasResults, Mode=OneWay}">
        <TextBlock Text="{x:Bind ViewModel.StatusText, Mode=OneWay}"
                   Foreground="{ThemeResource TextFillColorSecondaryBrush}"/>
      </Border>

      <!-- No Results Message -->
      <TextBlock Grid.Row="1"
                 Text="{x:Bind ViewModel.NoResultsText, Mode=OneWay}"
                 Visibility="{x:Bind ViewModel.ShowNoResults, Mode=OneWay}"
                 HorizontalAlignment="Center"
                 Margin="0,12,0,0"
                 Foreground="{ThemeResource TextFillColorSecondaryBrush}"/>

      <!-- Results Tree -->
      <ScrollViewer x:Name="ResultsScrollViewer"
                    Grid.Row="1"
                    Visibility="{x:Bind ViewModel.HasResults, Mode=OneWay}"
                    VerticalScrollBarVisibility="Auto"
                    HorizontalScrollBarVisibility="Disabled">
        <ItemsControl ItemsSource="{x:Bind ViewModel.FileResults, Mode=OneWay}"
                      Margin="0,4,0,0">
          <ItemsControl.ItemTemplate>
            <DataTemplate x:DataType="vm:SearchFileResultViewModel">
              <StackPanel>
                <!-- File Header -->
                <Grid Padding="4,4,12,4"
                      PointerPressed="FileHeader_PointerPressed"
                      PointerEntered="FileHeader_PointerEntered"
                      PointerExited="FileHeader_PointerExited"
                      Background="{x:Bind IsSelected, Mode=OneWay, Converter={StaticResource SelectionBackgroundConverter}}">
                  <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="36"/>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="Auto"/>
                  </Grid.ColumnDefinitions>

                  <!-- Expand/Collapse Button -->
                  <Button Grid.Column="0"
                          Command="{x:Bind ToggleExpandedCommand}"
                          Background="Transparent"
                          BorderThickness="0"
                          Padding="0"
                          HorizontalAlignment="Stretch"
                          VerticalAlignment="Stretch"
                          HorizontalContentAlignment="Center"
                          VerticalContentAlignment="Center">
                    <controls:Icon Symbol="{x:Bind IsExpanded, Mode=OneWay, Converter={StaticResource ExpandCollapseGlyphConverter}}"
                              FontSize="14"/>
                  </Button>

                  <!-- File Icon -->
                  <controls:FileIcon Grid.Column="1"
                                     IconDefinition="{x:Bind FileIcon}"
                                     Size="16"
                                     MinWidth="20"
                                     Margin="0,0,8,0"
                                     VerticalAlignment="Center"/>

                  <!-- File Name -->
                  <TextBlock Grid.Column="2"
                             Text="{x:Bind FileName}"
                             FontWeight="SemiBold"
                             TextTrimming="CharacterEllipsis"
                             VerticalAlignment="Center"
                             ToolTipService.ToolTip="{x:Bind RelativePath}"/>

                  <!-- Replace in File Button (visible on hover or selection when replace mode is enabled) -->
                  <Button Grid.Column="3"
                          Command="{x:Bind ReplaceInFileCommand}"
                          Visibility="{x:Bind ShowReplaceButton, Mode=OneWay}"
                          Padding="6,0"
                          Margin="4,0,0,0"
                          MinHeight="0"
                          VerticalAlignment="Stretch"
                          ToolTipService.ToolTip="{x:Bind ReplaceInFileTooltip}">
                    <controls:Icon Symbol="Replace" FontSize="12"/>
                  </Button>

                  <!-- Match Count Badge -->
                  <Border Grid.Column="4"
                          Background="{ThemeResource AccentFillColorDefaultBrush}"
                          CornerRadius="8"
                          Padding="6,1,6,2"
                          Margin="4,0,0,0"
                          VerticalAlignment="Center">
                    <TextBlock Text="{x:Bind MatchCount}"
                               Foreground="White"
                               VerticalAlignment="Center"
                               HorizontalAlignment="Center"/>
                  </Border>
                </Grid>

                <!-- Match Lines -->
                <ItemsControl ItemsSource="{x:Bind Matches}"
                              Visibility="{x:Bind IsExpanded, Mode=OneWay}">
                  <ItemsControl.ItemTemplate>
                    <DataTemplate x:DataType="vm:SearchMatchLineViewModel">
                      <Grid Padding="4,4,12,4"
                            PointerPressed="MatchLine_PointerPressed"
                            PointerEntered="MatchLine_PointerEntered"
                            PointerExited="MatchLine_PointerExited"
                            Background="{x:Bind IsSelected, Mode=OneWay, Converter={StaticResource SelectionBackgroundConverter}}">
                        <Grid.ColumnDefinitions>
                          <ColumnDefinition Width="36"/>
                          <ColumnDefinition Width="*"/>
                          <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>

                        <!-- Line Number -->
                        <TextBlock Grid.Column="0"
                                   Text="{x:Bind LineNumber}"
                                   Foreground="{ThemeResource TextFillColorSecondaryBrush}"
                                   HorizontalAlignment="Right"
                                   Margin="0,0,8,0"/>

                        <!-- Line Text with Highlighted Match -->
                        <TextBlock Grid.Column="1"
                                   TextTrimming="CharacterEllipsis"
                                   ToolTipService.ToolTip="{x:Bind LineText}">
                          <Run Text="{x:Bind TextBeforeMatch}"/><Run Text="{x:Bind MatchedText}" 
                               Foreground="{ThemeResource SearchMatchHighlightBrush}"/><Run Text="{x:Bind TextAfterMatch}"/>
                        </TextBlock>

                        <!-- Per-result Replace button (visible on hover or selection when replace mode is enabled) -->
                        <Button Grid.Column="2"
                                Command="{x:Bind ReplaceMatchCommand}"
                                Visibility="{x:Bind ShowReplaceButton, Mode=OneWay}"
                                Padding="6,0"
                                Margin="4,0,0,0"
                                MinHeight="0"
                                VerticalAlignment="Stretch"
                                ToolTipService.ToolTip="{x:Bind ReplaceMatchTooltip}">
                          <controls:Icon Symbol="Replace" FontSize="12"/>
                        </Button>
                      </Grid>
                    </DataTemplate>
                  </ItemsControl.ItemTemplate>
                </ItemsControl>
              </StackPanel>
            </DataTemplate>
          </ItemsControl.ItemTemplate>
        </ItemsControl>
      </ScrollViewer>
    </Grid>
  </Grid>
</UserControl>
