<UserControl
  x:Class="Celbridge.ProjectSettings.Views.FileEditorsSectionView"
  xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
  xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
  xmlns:vm="using:Celbridge.ProjectSettings.ViewModels"
  xmlns:services="using:Celbridge.UserInterface.Services">

  <UserControl.Resources>
    <services:BoolToVisibilityConverter x:Key="BoolToVisibilityConverter"/>
  </UserControl.Resources>

  <Grid>
    <ScrollViewer Padding="12"
                  Visibility="{x:Bind ViewModel.HasContent, Mode=OneWay, Converter={StaticResource BoolToVisibilityConverter}}">
      <ItemsControl ItemsSource="{x:Bind ViewModel.FileTypeRows}">
        <ItemsControl.ItemTemplate>
          <DataTemplate x:DataType="vm:FileTypeRowViewModel">
            <Grid Margin="0,3,0,3">
              <Grid.ColumnDefinitions>
                <ColumnDefinition Width="96"/>
                <ColumnDefinition Width="*"/>
              </Grid.ColumnDefinitions>
              <StackPanel Grid.Column="0" VerticalAlignment="Center">
                <TextBlock Text="{x:Bind Extension}"
                           Style="{StaticResource ListItemTitleTextStyle}"
                           TextTrimming="CharacterEllipsis"/>
                <TextBlock Text="{x:Bind TypeName}"
                           Style="{StaticResource SupportingTextStyle}"
                           TextTrimming="CharacterEllipsis"
                           Visibility="{x:Bind HasTypeName, Converter={StaticResource BoolToVisibilityConverter}}"/>
              </StackPanel>
              <ComboBox Grid.Column="1"
                        HorizontalAlignment="Stretch"
                        ToolTipService.ToolTip="{x:Bind EditorPickerTooltip}"
                        ItemsSource="{x:Bind Candidates}"
                        SelectedItem="{x:Bind SelectedCandidate, Mode=TwoWay}"/>
            </Grid>
          </DataTemplate>
        </ItemsControl.ItemTemplate>
      </ItemsControl>
    </ScrollViewer>

    <TextBlock Text="{x:Bind ViewModel.EmptyText, Mode=OneWay}"
               Margin="24"
               Foreground="{ThemeResource TextFillColorSecondaryBrush}"
               TextWrapping="Wrap"
               TextAlignment="Center"
               HorizontalAlignment="Center"
               VerticalAlignment="Center"
               Visibility="{x:Bind ViewModel.IsEmpty, Mode=OneWay, Converter={StaticResource BoolToVisibilityConverter}}"/>
  </Grid>
</UserControl>
