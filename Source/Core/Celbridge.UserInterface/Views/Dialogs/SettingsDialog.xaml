<ContentDialog
    x:Class="Celbridge.UserInterface.Views.SettingsDialog"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Celbridge.UserInterface.Views"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    Title="{x:Bind TitleString}"
    CloseButtonText="{x:Bind CloseString}"
    DefaultButton="Close">

  <ContentDialog.Resources>
    <!-- The default caps size a dialog for a prompt, which the settings sections do not fit into. Scoped
         here so the prompts keep the default. -->
    <x:Double x:Key="ContentDialogMaxWidth">900</x:Double>
    <x:Double x:Key="ContentDialogMaxHeight">720</x:Double>
  </ContentDialog.Resources>

  <ScrollViewer MinWidth="784"
                VerticalScrollBarVisibility="Auto"
                HorizontalScrollMode="Disabled">

    <StackPanel Spacing="24"
                Margin="0,8,16,8"
                HorizontalAlignment="Left">

      <Grid ColumnSpacing="24">
        <Grid.ColumnDefinitions>
          <ColumnDefinition Width="160"/>
          <ColumnDefinition Width="Auto"/>
        </Grid.ColumnDefinitions>

        <TextBlock Text="{x:Bind ApplicationThemeString}"
                   VerticalAlignment="Center"/>

        <ComboBox Grid.Column="1"
                  VerticalAlignment="Center"
                  HorizontalAlignment="Left"
                  ItemsSource="{x:Bind ViewModel.ThemeOptions}"
                  SelectedItem="{x:Bind ViewModel.SelectedTheme, Mode=TwoWay}"
                  DisplayMemberPath="DisplayName"/>
      </Grid>

      <local:WorkshopSettingsView Margin="0,24,0,0"/>

      <local:PrivacySettingsView Margin="0,24,0,0"/>

    </StackPanel>
  </ScrollViewer>
</ContentDialog>
