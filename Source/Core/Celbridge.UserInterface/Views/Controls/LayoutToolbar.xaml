<UserControl
    x:Class="Celbridge.UserInterface.Views.LayoutToolbar"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:controls="using:Celbridge.UserInterface.Views.Controls"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:icons="using:Celbridge.UserInterface.Views.PanelIcons">

    <StackPanel x:Name="ApplicationLayoutToolbar"
                Orientation="Horizontal"
                HorizontalAlignment="Right"
                VerticalAlignment="Stretch">
        
        <!-- Panel Layout Button with Flyout -->
        <!-- A plain button, not a toggle: an open flyout is a passing state, not a selection. -->
        <controls:IconButton x:Name="PanelLayoutButton"
                             AutomationProperties.AutomationId="panel-layout-button"
                             VerticalAlignment="Center"
                             DoubleTapped="Button_DoubleTapped"
                             Margin="0,0,2,0">
            <Button.Flyout>
                <Flyout x:Name="PanelLayoutFlyout" Placement="Bottom">
                    <StackPanel Width="250" Spacing="8">
                        <!-- Window Mode Selection -->
                        <TextBlock x:Name="WindowModeHeader" 
                                   FontWeight="SemiBold" 
                                   Margin="0,4,0,8"/>
                        
                        <RadioButton x:Name="DefaultModeRadio"
                                     GroupName="LayoutMode"
                                     Checked="LayoutModeRadio_Checked">
                            <StackPanel Orientation="Horizontal" Spacing="8">
                                <controls:Icon
                                          Symbol="Windowed"
                                          FontSize="{StaticResource IconSizeMedium}"/>
                                <TextBlock x:Name="DefaultModeLabel"/>
                            </StackPanel>
                        </RadioButton>

                        <RadioButton x:Name="FocusModeRadio"
                                     GroupName="LayoutMode"
                                     Checked="LayoutModeRadio_Checked">
                            <StackPanel Orientation="Horizontal" Spacing="8">
                                <controls:Icon
                                          Symbol="FocusMode"
                                          FontSize="{StaticResource IconSizeMedium}"/>
                                <TextBlock x:Name="FocusModeLabel"/>
                            </StackPanel>
                        </RadioButton>

                        <RadioButton x:Name="PresentationModeRadio"
                                     GroupName="LayoutMode"
                                     Checked="LayoutModeRadio_Checked">
                            <StackPanel Orientation="Horizontal" Spacing="8">
                                <controls:Icon
                                          Symbol="Presentation"
                                          FontSize="{StaticResource IconSizeMedium}"/>
                                <TextBlock x:Name="PresentationModeLabel"/>
                            </StackPanel>
                        </RadioButton>

                        <!-- Full Screen toggle. Separate from the layout modes (fullscreen is orthogonal
                             to chrome level). Windows only; macOS uses the native title-bar control, so
                             this is collapsed on non-Windows in code-behind. -->
                        <ToggleButton x:Name="FullScreenToggle"
                                      Click="FullScreenToggle_Click"
                                      HorizontalAlignment="Stretch"
                                      HorizontalContentAlignment="Left"
                                      Background="Transparent"
                                      BorderThickness="0">
                            <StackPanel Orientation="Horizontal" Spacing="8">
                                <controls:Icon
                                          Symbol="FullScreen"
                                          FontSize="{StaticResource IconSizeMedium}"/>
                                <TextBlock x:Name="FullScreenLabel"/>
                            </StackPanel>
                        </ToggleButton>

                        <!-- Bottom Panel Alignment. The options are mutually exclusive and each one is
                             described in its tooltip, so they read as a row of glyphs rather than four
                             labelled rows. The flyout deliberately stays open on a click, so the user can
                             step through them and watch the layout change behind it. -->
                        <TextBlock x:Name="BottomAreaAlignmentHeader"
                                   Margin="0,4,0,0"/>

                        <StackPanel x:Name="BottomAreaAlignmentButtons"
                                    Orientation="Horizontal"
                                    Spacing="4">
                            <controls:IconToggleButton x:Name="AlignBottomAreaLeftButton"
                                                       Click="BottomAreaAlignmentButton_Click"
                                                       Padding="8,6"
                                                       MinWidth="36">
                                <icons:BottomAreaAlignmentIcon Alignment="Left"/>
                            </controls:IconToggleButton>

                            <controls:IconToggleButton x:Name="AlignBottomAreaCenterButton"
                                                       Click="BottomAreaAlignmentButton_Click"
                                                       Padding="8,6"
                                                       MinWidth="36">
                                <icons:BottomAreaAlignmentIcon Alignment="Center"/>
                            </controls:IconToggleButton>

                            <controls:IconToggleButton x:Name="AlignBottomAreaRightButton"
                                                       Click="BottomAreaAlignmentButton_Click"
                                                       Padding="8,6"
                                                       MinWidth="36">
                                <icons:BottomAreaAlignmentIcon Alignment="Right"/>
                            </controls:IconToggleButton>

                            <controls:IconToggleButton x:Name="AlignBottomAreaJustifyButton"
                                                       Click="BottomAreaAlignmentButton_Click"
                                                       Padding="8,6"
                                                       MinWidth="36">
                                <icons:BottomAreaAlignmentIcon Alignment="Justify"/>
                            </controls:IconToggleButton>
                        </StackPanel>

                        <MenuFlyoutSeparator/>

                        <!-- Reset Layout Button -->
                        <Button Click="ResetLayoutButton_Click"
                                HorizontalAlignment="Stretch">
                            <TextBlock x:Name="ResetLayoutButtonText"/>
                        </Button>
                    </StackPanel>
                </Flyout>
            </Button.Flyout>
            <icons:PanelLayoutIcon x:Name="PanelLayoutIcon"/>
        </controls:IconButton>
        
        <!-- Panel Toggle Buttons -->
        <StackPanel x:Name="PanelToggleButtons"
                    Orientation="Horizontal"
                    VerticalAlignment="Stretch">
            <controls:IconButton x:Name="ToggleUtilityPanelButton"
                                 AutomationProperties.AutomationId="explorer-toggle-button"
                                 VerticalAlignment="Center"
                                 Click="ToggleUtilityPanelButton_Click"
                                 DoubleTapped="Button_DoubleTapped"
                                 Margin="2,0">
                <icons:UtilityPanelIcon x:Name="UtilityPanelIcon"/>
            </controls:IconButton>
            
            <controls:IconButton x:Name="ToggleBottomAreaButton"
                                 AutomationProperties.AutomationId="bottom-area-toggle-button"
                                 VerticalAlignment="Center"
                                 Click="ToggleBottomAreaButton_Click"
                                 DoubleTapped="Button_DoubleTapped"
                                 Margin="2,0">
                <icons:BottomAreaIcon x:Name="BottomAreaIcon"/>
            </controls:IconButton>
            
            <controls:IconButton x:Name="ToggleSideAreaButton"
                                 AutomationProperties.AutomationId="side-area-toggle-button"
                                 VerticalAlignment="Center"
                                 Click="ToggleSideAreaButton_Click"
                                 DoubleTapped="Button_DoubleTapped"
                                 Margin="2,0">
                <icons:SideAreaIcon x:Name="SideAreaIcon"/>
            </controls:IconButton>
        </StackPanel>
        
    </StackPanel>
</UserControl>
