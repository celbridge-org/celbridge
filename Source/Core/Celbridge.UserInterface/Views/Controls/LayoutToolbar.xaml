<UserControl
    x:Class="Celbridge.UserInterface.Views.LayoutToolbar"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:controls="using:Celbridge.UserInterface.Views.Controls"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:icons="using:Celbridge.UserInterface.Views.PanelIcons">

    <StackPanel x:Name="ApplicationLayoutToolbar"
                Orientation="Horizontal"
                HorizontalAlignment="Right"
                VerticalAlignment="Center">
        
        <!-- Panel Layout Button with Flyout -->
        <Button x:Name="PanelLayoutButton"
                Click="PanelLayoutButton_Click"
                DoubleTapped="Button_DoubleTapped"
                Margin="0, 0, 2, 0"
                Padding="6"
                Background="Transparent"
                BorderThickness="0">
            <Button.Flyout>
                <Flyout x:Name="PanelLayoutFlyout" Placement="Bottom">
                    <StackPanel Width="250" Spacing="8">
                        <!-- Window Mode Selection -->
                        <TextBlock x:Name="WindowModeHeader" 
                                   FontWeight="SemiBold" 
                                   Margin="0,4,0,8"/>
                        
                        <RadioButton x:Name="DefaultModeRadio"
                                     GroupName="LayoutMode"
                                     Checked="LayoutModeRadio_Checked">
                            <StackPanel Orientation="Horizontal" Spacing="8">
                                <controls:Icon
                                          Symbol="Windowed"
                                          FontSize="20"/>
                                <TextBlock x:Name="DefaultModeLabel"/>
                            </StackPanel>
                        </RadioButton>

                        <RadioButton x:Name="FocusModeRadio"
                                     GroupName="LayoutMode"
                                     Checked="LayoutModeRadio_Checked">
                            <StackPanel Orientation="Horizontal" Spacing="8">
                                <controls:Icon
                                          Symbol="FocusMode"
                                          FontSize="20"/>
                                <TextBlock x:Name="FocusModeLabel"/>
                            </StackPanel>
                        </RadioButton>

                        <RadioButton x:Name="PresentationModeRadio"
                                     GroupName="LayoutMode"
                                     Checked="LayoutModeRadio_Checked">
                            <StackPanel Orientation="Horizontal" Spacing="8">
                                <controls:Icon
                                          Symbol="Presentation"
                                          FontSize="20"/>
                                <TextBlock x:Name="PresentationModeLabel"/>
                            </StackPanel>
                        </RadioButton>

                        <!-- Full Screen toggle. Separate from the layout modes (fullscreen is orthogonal
                             to chrome level). Windows only; macOS uses the native title-bar control, so
                             this is collapsed on non-Windows in code-behind. -->
                        <ToggleButton x:Name="FullScreenToggle"
                                      Click="FullScreenToggle_Click"
                                      HorizontalAlignment="Stretch"
                                      HorizontalContentAlignment="Left"
                                      Background="Transparent"
                                      BorderThickness="0">
                            <StackPanel Orientation="Horizontal" Spacing="8">
                                <controls:Icon
                                          Symbol="FullScreen"
                                          FontSize="20"/>
                                <TextBlock x:Name="FullScreenLabel"/>
                            </StackPanel>
                        </ToggleButton>

                        <MenuFlyoutSeparator x:Name="ResetLayoutSeparator"/>

                        <!-- Reset Layout Button -->
                        <Button x:Name="ResetLayoutButton"
                                Click="ResetLayoutButton_Click"
                                HorizontalAlignment="Stretch">
                            <TextBlock x:Name="ResetLayoutButtonText"/>
                        </Button>
                    </StackPanel>
                </Flyout>
            </Button.Flyout>
            <icons:PanelLayoutIcon x:Name="PanelLayoutIcon"/>
        </Button>
        
        <!-- Panel Toggle Buttons -->
        <StackPanel x:Name="PanelToggleButtons" Orientation="Horizontal">
            <!-- Primary Panel Toggle -->
            <Button x:Name="TogglePrimaryPanelButton"
                    AutomationProperties.AutomationId="landmark.toggle-explorer"
                    Click="TogglePrimaryPanelButton_Click"
                    DoubleTapped="Button_DoubleTapped"
                    Margin="2,0"
                    Padding="6"
                    Background="Transparent"
                    BorderThickness="0">
                <icons:PrimaryPanelIcon x:Name="PrimaryPanelIcon"/>
            </Button>
            
            <!-- Console Panel Toggle -->
            <Button x:Name="ToggleConsolePanelButton"
                    AutomationProperties.AutomationId="landmark.toggle-console"
                    Click="ToggleConsolePanelButton_Click"
                    DoubleTapped="Button_DoubleTapped"
                    Margin="2,0"
                    Padding="6"
                    Background="Transparent"
                    BorderThickness="0">
                <icons:ConsolePanelIcon x:Name="ConsolePanelIcon"/>
            </Button>
            
            <!-- Secondary Panel Toggle -->
            <Button x:Name="ToggleSecondaryPanelButton"
                    AutomationProperties.AutomationId="landmark.toggle-inspector"
                    Click="ToggleSecondaryPanelButton_Click"
                    DoubleTapped="Button_DoubleTapped"
                    Margin="2,0"
                    Padding="6"
                    Background="Transparent"
                    BorderThickness="0">
                <icons:SecondaryPanelIcon x:Name="SecondaryPanelIcon"/>
            </Button>
        </StackPanel>
        
    </StackPanel>
</UserControl>
