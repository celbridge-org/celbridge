<UserControl
    x:Name="IconRoot"
    x:Class="Celbridge.UserInterface.Views.PanelIcons.PanelLayoutIcon"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    
    <Viewbox Width="{StaticResource IconSizeMedium}" Height="{StaticResource IconSizeMedium}">
        <Grid Width="16" Height="16">

            <!-- Left tall rectangle (primary panel) -->
            <Rectangle Width="7" Height="16"
                       HorizontalAlignment="Left"
                       VerticalAlignment="Center"
                       Stroke="{Binding Foreground, ElementName=IconRoot}"
                       StrokeThickness="1.0"
                       RadiusX="2" RadiusY="2"/>
            
            <!-- Top right rectangle (documents panel) -->
            <Rectangle Width="7" Height="7"
                       HorizontalAlignment="Right"
                       VerticalAlignment="Top"
                       Stroke="{Binding Foreground, ElementName=IconRoot}"
                       StrokeThickness="1.0"
                       RadiusX="2" RadiusY="2"/>

            <!-- Bottom right rectangle (console/secondary panel) -->
            <Rectangle Width="7" Height="7"
                       HorizontalAlignment="Right"
                       VerticalAlignment="Bottom"
                       Stroke="{Binding Foreground, ElementName=IconRoot}"
                       StrokeThickness="1.0"
                       RadiusX="2" RadiusY="2"/>

        </Grid>
    </Viewbox>
</UserControl>
