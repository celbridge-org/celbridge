<UserControl
    x:Class="Celbridge.UserInterface.Views.Controls.Splitter"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006">

  <UserControl.Resources>
    <!-- Fade in animation for hover - slightly delayed, smooth fade -->
    <Storyboard x:Name="HoverFadeIn">
      <DoubleAnimation Storyboard.TargetName="HoverLine"
                       Storyboard.TargetProperty="Opacity"
                       To="1.0"
                       BeginTime="0:0:0.20"
                       Duration="0:0:0.20">
        <DoubleAnimation.EasingFunction>
          <QuadraticEase EasingMode="EaseOut"/>
        </DoubleAnimation.EasingFunction>
      </DoubleAnimation>
    </Storyboard>
    
    <!-- Fade out animation - quick fade. From is set in code from the current opacity so the
         fade-out animates down smoothly even though stopping the fade-in resets opacity to 0. -->
    <Storyboard x:Name="HoverFadeOut">
      <DoubleAnimation x:Name="HoverFadeOutAnimation"
                       Storyboard.TargetName="HoverLine"
                       Storyboard.TargetProperty="Opacity"
                       To="0.0"
                       Duration="0:0:0.20">
        <DoubleAnimation.EasingFunction>
          <QuadraticEase EasingMode="EaseIn"/>
        </DoubleAnimation.EasingFunction>
      </DoubleAnimation>
    </Storyboard>
  </UserControl.Resources>

  <!-- Grab area, filled with GrabAreaBrush and holding the accent line that appears on hover and drag. The
       background is never null, so the area stays hit testable whether it is filled or transparent. A filled
       grab area edges its leading side so it reads apart from whatever sits against it, which for a document
       splitter can be arbitrary web content. -->
  <Border x:Name="SplitterBorder"
          Background="Transparent"
          BorderBrush="{ThemeResource PanelEdgeBrush}">
    <Grid>
      <!-- Hidden at rest, thickened to the accent while dragging -->
      <Rectangle x:Name="SplitterLine"
                 Fill="{ThemeResource DividerBrush}"/>
      <!-- Accent hover line - fades in on hover -->
      <Rectangle x:Name="HoverLine"
                 Fill="{ThemeResource AccentBrush}"
                 Opacity="0"/>
    </Grid>
  </Border>
</UserControl>
