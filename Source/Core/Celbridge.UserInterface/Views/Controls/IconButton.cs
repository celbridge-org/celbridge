namespace Celbridge.UserInterface.Views.Controls;

/// <summary>
/// A flat icon button, carrying the shared icon button appearance as its own. Use it wherever a button's
/// content is an icon, so a call site cannot land on the framework's default chrome by omitting a style.
/// </summary>
public sealed class IconButton : Button
{
}
