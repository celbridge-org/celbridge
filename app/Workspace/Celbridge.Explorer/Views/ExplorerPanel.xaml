<UserControl
  x:Class="Celbridge.Explorer.Views.ExplorerPanel"
  xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
  xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
  xmlns:local="using:Celbridge.Explorer.Views"
  xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
  DataContext="{x:Bind ViewModel}"
  Background="Transparent"
  PointerEntered="UserControl_PointerEntered"
  PointerExited="UserControl_PointerExited"
  GotFocus="UserControl_GotFocus"
  LostFocus="UserControl_LostFocus">

  <Grid VerticalAlignment="Stretch"
        Background="Transparent">
    <Grid.RowDefinitions>
      <RowDefinition Height="40"/>
      <RowDefinition Height="*"/>
    </Grid.RowDefinitions>

    <!-- Title Bar -->
    <Grid Grid.Row="0"
          BorderBrush="{ThemeResource PanelBorderBrush}"
          BorderThickness="0,0,0,1">
      <Grid.ColumnDefinitions>
        <ColumnDefinition Width="Auto"/>
        <ColumnDefinition Width="*"/>
      </Grid.ColumnDefinitions>

      <TextBlock Grid.Column="0"
                 Text="{x:Bind ViewModel.TitleText, Mode=OneWay}"
                 Margin="12,0,0,0"
                 VerticalAlignment="Center"/>
    </Grid>

    <!-- Resource Tree View -->
    <local:ResourceTreeView x:Name="ResourceTreeView"
                            Grid.Row="1"/>
  </Grid>
</UserControl>
